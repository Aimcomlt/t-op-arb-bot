{
  "name": "t-op-arb-bot",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["backend", "frontend", "packages/types"],
  "scripts": {
    "dev": "concurrently \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "pnpm -r format"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "husky": "^9.1.7",
    "prettier": "^3.6.2"
  }
}
